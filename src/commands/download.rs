use crate::config::Config;
use crate::error::Result;

pub fn do_download(_config: &Config) -> Result<()> {
    /*
    let response = get("https://httpbin.org/ip")?.json::<HttpBinIPResponse>()?;
    println!("{:#?}", response);
    */
    todo!()
}
